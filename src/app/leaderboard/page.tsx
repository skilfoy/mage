import { leaderboard } from "@/lib/analytics";
export default async function Leaderboard(){ const entries=await leaderboard(); return <div className="space-y-5"><h1 className="text-4xl font-black">Leaderboard</h1><div className="card overflow-x-auto"><table className="w-full text-left"><thead><tr className="text-slate-300"><th>Name</th><th>Score</th><th>Completed</th><th>Updated</th></tr></thead><tbody>{entries.map(e=><tr key={e.displayName+e.updatedAt.toISOString()} className="border-t border-white/10"><td className="py-2">{e.displayName}</td><td>{e.totalScore}</td><td>{e.completedLevels}</td><td>{e.updatedAt.toLocaleString()}</td></tr>)}</tbody></table></div><a className="btn" href="/map">Back to map</a></div>; }
