"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { Disclaimer } from "@/components/Disclaimer";
export default function Start(){ const [displayName,setDisplayName]=useState(""); const [error,setError]=useState(""); const router=useRouter(); async function submit(e:React.FormEvent){ e.preventDefault(); setError(""); const res=await fetch("/api/session/start",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({displayName})}); const json=await res.json(); if(!json.ok) setError(json.error.message); else router.push("/map"); } return <div className="mx-auto max-w-2xl space-y-6"><h1 className="text-4xl font-black">Start PromptLock Academy</h1><Disclaimer/><div className="card space-y-4"><p className="text-slate-300">Use only this authorized lab. Your prompts are redacted by default for privacy-preserving analytics.</p><form onSubmit={submit} className="space-y-3"><label className="block font-semibold" htmlFor="displayName">Display name</label><input id="displayName" className="input" value={displayName} onChange={e=>setDisplayName(e.target.value)} maxLength={40} required/><button className="btn" type="submit">Create session</button>{error && <p className="text-red-300" role="alert">{error}</p>}</form></div></div>; }
