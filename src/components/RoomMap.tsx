import Link from "next/link";
type Rooms = {id:string;slug:string;title:string;description:string;levels:{id:string;slug:string;title:string;difficulty:string;status:string;score:number;learningObjective:string}[]}[];
export function RoomMap({rooms}:{rooms:Rooms}){ return <div className="space-y-6">{rooms.map(room=><section key={room.id} className="card"><h2 className="text-2xl font-black">{room.title}</h2><p className="text-slate-300">{room.description}</p><div className="mt-4 grid gap-3 md:grid-cols-3">{room.levels.map(level=><article key={level.id} className="rounded-xl border border-white/10 bg-black/20 p-4"><div className="flex items-center justify-between"><h3 className="font-bold">{level.title}</h3><span className="rounded-full bg-white/10 px-2 py-1 text-xs">{level.status}</span></div><p className="mt-2 text-sm text-slate-300">{level.learningObjective}</p><p className="mt-2 text-xs text-slate-400">{level.difficulty} · {level.score} pts</p>{level.status!=="locked" ? <Link className="btn mt-3 w-full" href={`/level/${level.slug}`}>{level.status==="completed"?"Review":"Enter"}</Link> : <button className="btn mt-3 w-full" disabled>Locked</button>}</article>)}</div></section>)}</div>; }
